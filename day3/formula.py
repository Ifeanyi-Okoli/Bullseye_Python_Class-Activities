import math 
import sympy

#Define the functions ψ(x, y) and ψ(x, y)
x, y = sympy.symbols("x y")
ϕ = x**2 - y**2
ψ = 2*x*y

#compute the partial derivatives
# ∂ϕ∂x = sympy.diff(ϕ, x)
# ∂ϕ∂y = sympy.diff(ϕ, y)
# ∂ψ∂x = sympy.diff(ψ, x)
# ∂ψ∂y = sympy.diff(ψ, y)




